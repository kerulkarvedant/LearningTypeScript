type Status = "pending" | "success" | "failed";

let status: Status = "pending";

status = "success";

console.log(status);


// Union Types

// Union means:

// This value can be one of multiple types. Union Types

//We use |.

