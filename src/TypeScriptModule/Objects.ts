let product: {
    name: string;
    price: number;
    available: boolean;
} = {
    name: "Laptop",
    price: 50000,
    available: true
};

console.log(product.name);
console.log(product.price);
console.log(product.available);