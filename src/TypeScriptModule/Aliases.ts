type User = {
  id: number;
  name: string;
  email: string;
};

const user: User = {
  id: 1,
  name: "Vedant",
  email: "vedant@example.com",
};

// Type Aliases

// A type alias is another way of giving a name to a type.